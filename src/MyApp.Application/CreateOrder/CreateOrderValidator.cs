// CreateOrderCommandValidator is emitted at compile time by the
// ZeroAlloc.Validation source generator from the [Validate] + per-property
// attributes on CreateOrderCommand and OrderItem in CreateOrderCommand.cs.
//
// Add hand-written rules here by declaring a partial class with the same name
// (e.g. `public sealed partial class CreateOrderCommandValidator { ... }`)
// to extend the generated validator without losing source-gen wiring.
