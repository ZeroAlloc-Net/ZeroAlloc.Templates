// Empty shell for now — BenchmarkDotNet fixtures land in Task 7.
// Keeping the executable entry point alive so the project builds + can be
// invoked under `--job dry` once fixtures land.
Console.WriteLine("MyApp.Benchmarks shell — see Task 7 for fixtures.");
