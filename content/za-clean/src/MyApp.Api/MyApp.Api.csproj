<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>MyApp.Api</RootNamespace>
    <!-- ZAM003: class request types are fine here. -->
    <NoWarn>$(NoWarn);ZAM003</NoWarn>
    <PublishAot>true</PublishAot>
    <InvariantGlobalization>true</InvariantGlobalization>
    <!-- Collapse IL2xxx/IL3xxx warnings per source assembly into a single
         summary line. Expected warnings come from EF Core (model building
         hooks), OpenTelemetry (RequiresDynamicCode for OTel SDK reflection)
         and SQLite (DependencyContext.LoadDefault under single-file). All
         either guarded at runtime (we use the compiled model + embedded
         schema script) or invoked only outside the hot path. -->
    <TrimmerSingleWarn>true</TrimmerSingleWarn>
  </PropertyGroup>
  <ItemGroup>
    <!-- Embed the schema script so the AOT binary can create the database without
         needing the design-time EF Core pipeline (which is not AOT-compatible).
         Regenerate via `dotnet ef migrations script -o src/MyApp.Api/schema.sql`. -->
    <EmbeddedResource Include="schema.sql" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
    <!-- ZeroAlloc.Authorization is NOT referenced directly here; it flows transitively
         through MyApp.Application's ProjectReference. Application's PackageReference
         marks its analyzer asset as PrivateAssets to block transitive outflow.
         ZeroAlloc.Mediator.Authorization stays as a direct PackageReference (Api uses
         WithAuthorization + ISecurityContextAccessor), but its analyzer asset is
         excluded to prevent the v2 ZA.Authorization analyzer (a transitive dep of
         Mediator.Authorization itself) from being picked up by the SDK's analyzer-
         resolution path and emitting a duplicate
         GeneratedAuthorizationRegistration.AddZeroAllocAuthorization here. With both
         the direct-ref drop AND the analyzer-exclude in place, the generator runs
         exactly once — in MyApp.Application, which owns the [Policy] / [RequirePolicy]
         types. Tracked as upstream fix candidate: ZA.Mediator.Authorization v2.0.1
         should set PrivateAssets=analyzers on its own ZA.Authorization PackageReference
         so this template-side workaround becomes unnecessary. -->
    <PackageReference Include="ZeroAlloc.Mediator.Authorization">
      <ExcludeAssets>analyzers</ExcludeAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="ZeroAlloc.Mapping">
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="OpenTelemetry.Exporter.Console" />
    <PackageReference Include="OpenTelemetry.Exporter.OpenTelemetryProtocol" />
    <PackageReference Include="OpenTelemetry.Extensions.Hosting" />
    <PackageReference Include="OpenTelemetry.Instrumentation.AspNetCore" />
    <PackageReference Include="OpenTelemetry.Instrumentation.Http" />
    <PackageReference Include="OpenTelemetry.Instrumentation.EntityFrameworkCore" />
    <ProjectReference Include="../MyApp.Application/MyApp.Application.csproj" />
    <ProjectReference Include="../MyApp.Infrastructure/MyApp.Infrastructure.csproj" />
  </ItemGroup>
</Project>
